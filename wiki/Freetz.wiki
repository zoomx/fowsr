#summary Freetz build instructions.

= Introduction =

The *fowsr* application is included as a [http://freetz.org/browser/trunk/make/fowsr package] in [http://freetz.org/wiki# Freetz].

= Details =

== Custom build ==

Download the Freetz build system and generate the binaries using the following steps:

 * Check the [http://freetz.org/wiki/help/howtos/common/install#NotwendigePakete prerequisites] for running the Freetz build system.

 * Download the latest Freetz trunk:

      `$ svn co http://svn.freetz.org/trunk freetz`

 * Enter the freetz catalog:

      `$ cd freetz`

 * Start the build system:

      $ `make menuconfig`

 * Select your box and select 'fowsr' to be included in the image. Exit the build system and save the changes.

 * Build the code:

      $ `make`